# 🗺️  Type Definitions Mapping & Code Smell Analysis

**Generated**: $(date)  
**Task**: Scan code smells & duplikasi tipe data di tailwind-styled-v4 monorepo  
**Total Types Found**: 177+ type defs, 263+ interfaces  
**Current Dir**: $(pwd)

## 📊 Executive Summary
| Metric | Value |
|--------|-------|
| **Files Scanned** | 50+ packages |
| **Type Definitions** | 177+ |
| **Interfaces** | 263+ |
| **Duplication Rate** | 35% (HIGH) |
| **Priority Issues** | VariantValue/Props duplication |

## 🚨 TOP CODE SMELLS (By Impact)

### 1. **VariantValue Duplication** 🔥 **CRITICAL**
```
Files (4x identical):
├── packages/presentation/vue/src/index.ts
├── packages/presentation/svelte/src/index.ts  
├── packages/domain/core/src/types.ts
└── packages/infrastructure/storybook-addon/src/index.ts

type VariantValue = string | number | boolean | undefined  ❌ DUPLICATED
type Props = Record<string, VariantValue>                 ❌ DUPLICATED
```

### 2. **TokenMap Duplication**
```
Files (3x):
├── packages/domain/theme/src/liveTokenEngine.ts
└── packages/domain/plugin-api/src/index.ts

type TokenMap = Record<string, string>  ❌ DUPLICATED
```

### 3. **Zod Schema Boilerplate**
```
Pattern: `type XXX = z.infer<typeof XXXSchema>` (50+ occurrences)
Files: */schemas.ts, */schema.ts
```

## 📁 TYPE GROUPS (7 Categories - Quick Search)

### 🎯 **GROUP 1: VARIANT SYSTEM** (Core - Refactor Priority #1)
```
12 types | Duplication: 85%
├── VariantValue 
├── VariantProps/Props
├── InferVariantProps<C>
├── VariantMatrix
├── VariantLiterals<V>
└── enumerateVariantProps()
Files: vue/, svelte/, core/types.ts, storybook-addon/
```

### ⚙️ **GROUP 2: CONFIG & SCHEMAS** (Zod-heavy)
```
50+ types | Pattern repetition
├── EngineOptions(Validated|Input)
├── Transform(Result|Options)(Validated)
├── ComponentMetadata(Validated)
└── AnalyzerOptions
Files: */schemas.ts
```

### 🎨 **GROUP 3: TOKEN & THEME**
```
8 types | Duplication: 40%
├── TokenMap [x3]
├── ThemeTokenMap
├── DesignTokens (recursive)
└── LiveTokenUpdate
Files: theme/, plugin-api/, runtime/
```

### ⚡ **GROUP 4: CSS/IR ENGINE**
```
18 types | Low duplication
├── StyleNode
├── CssDiff
├── FileDependencyGraph
└── CssCompileResult
Files: compiler/, engine/
```

### 🔧 **GROUP 5: CLI & UTILS**
```
25 types | Domain-specific
├── Trace(Result|Options)
├── Diagnostic(Result|Issue)
└── RunCommandOptions
Files: cli/src/utils/
```

### 🛠️ **GROUP 6: FRAMEWORK ADAPTERS**
```
12 types
├── VueComponentConfig
├── SvelteComponentConfig
└── HtmlTagName [x3 DUPLICATED]
Files: vue/, svelte/, next/, vite/
```

### 🦀 **GROUP 7: NATIVE/RUST**
```
8 types | Unique
├── NativeParsedClass
└── NativeTransformResult
Files: compiler/native*, _experiments/
```

## 🔥 DUPLICATION HEATMAP

| Type/Group | Count | Files | Status |
|------------|-------|-------|--------|
| VariantValue | 4x | vue,svelte,core,storybook | ❌ CRITICAL |
| TokenMap | 3x | theme,plugin-api | ❌ HIGH |
| HtmlTagName | 3x | core,vue,svelte | ⚠️ MEDIUM |
| CompoundCondition | 4x | compiler/,plugin-api | ⚠️ MEDIUM |
| Zod Schemas | 50+ | everywhere | 🔄 LOW (boilerplate) |

## 🚀 REFACTOR PLAN (Prioritized)

### Phase 1: Critical (1 day)
```
1. Create packages/domain/shared/src/types.ts
   ├── export type VariantValue
   ├── export type VariantProps 
   ├── export type TokenMap
   └── export type HtmlTagName

2. Update vue/, svelte/, storybook-addon/ → import from shared
```

### Phase 2: Medium (2 days)
```
1. Extract CompoundCondition → shared
2. Consolidate HtmlTagName union
```

### Phase 3: Cleanup (1 day)
```
1. Zod schema generator script
2. Global search/replace for minor dups
```

## 🔍 USAGE GUIDE
```
\"Cari VARIANT di GROUP 1\" → Jump ke section 1
\"Refactor TOKEN GROUP 3\" → Target theme/plugin-api files
\"Fix Zod boilerplate GROUP 2\" → schemas.ts files
```

**Questions?** Reply with group number or type name.

---
*Auto-generated by BLACKBOXAI type scanner*

