# v4.5 Platform Mode (Concept Densification)

v4.5 memadatkan ide dari sekadar kumpulan tooling menjadi pengalaman platform yang konsisten lewat `tw`.

## Command surface

- `tw studio`
- `tw deploy [name]`
- `tw ai "prompt"`
- `tw sync <init|pull|push>`
- `tw audit`
- `tw share <name>`

## Prinsip implementasi saat ini

1. **Buildable-first**: command baru harus tetap ringan dan tidak merusak workflow existing.
2. **Backward compatible**: command lama (`tailwind-styled`, `create-tailwind-styled`) tetap hidup.
3. **Script-driven prototypes**: fitur platform awal dieksekusi via `scripts/v45/*` supaya iterasi cepat.

## Next increment

- Integrasi AI provider (Ollama/OpenAI) opsional, fallback lokal default.
- Sinkronisasi token remote provider.
- Audit ruleset lebih dalam (security + a11y + performance) per package.

---

## Status Implementasi (v4.2.0 — 2026-03-16)

| Command | Status | Catatan |
|---------|--------|---------|
| `tw studio` | ✅ Production | Spawn studio.mjs langsung; `--port`, `--mode` |
| `tw deploy [name]` | ✅ Production | Manifest ke `.tw-cache/`, `--dry-run` |
| `tw ai "prompt"` | ✅ Production | Multi-provider: Anthropic/OpenAI/Ollama + static fallback |
| `tw sync <init\|pull\|push\|diff>` | ✅ Production | W3C DTCG format |
| `tw sync figma <pull\|push\|diff>` | ✅ Production | Figma Variables API |
| `tw audit` | ✅ Production | Deprecated, a11y, security, performance — `--scope`, `--json` |
| `tw share <n>` | ✅ Production | Share payload dengan installCommand, importExample |

### Sprint 6 — Done (2026-03-16)
- `tw deploy --registry=URL` ✅ — HTTP publish ke registry
- `tw registry serve|list|info` ✅ — local/team registry server
- `tw sync pull --from=URL` ✅ — remote URL pull (HTTP/HTTPS)
- `tw sync push --to-url=URL` ✅ — remote HTTP push
- `tw cluster-server` ✅ — remote build worker server
- `routeCssMiddleware` ✅ — Next.js `<link>` injection dari manifest
- Vite plugin `routeCss: true` ✅

### Sprint 7+ (selanjutnya)
- AI provider tambahan (Google Gemini, Ollama new models)
- RSC auto-inject route CSS
- Figma multi-mode support
- Metrics persistence
