# v4.3 Command Densification

Tujuan v4.3 adalah memadatkan entrypoint command menjadi satu CLI utama: `tw`.

## Ringkasan

- `tw plugin search|list|install`
- `tw create <name> --template=...`
- `tw dashboard --port=...`
- `tw test [--watch]`
- `tw storybook --variants='{...}'`
- `tw code --docs|--install`

## Catatan kompatibilitas

- Command lama (`tailwind-styled`, `create-tailwind-styled`) tetap tersedia.
- `tw` menjadi alias entrypoint baru agar discoverability lebih sederhana.

---

## Status Implementasi (v4.2.0 — 2026-03-16)

| Command | Status | Catatan |
|---------|--------|---------|
| `tw plugin search\|list\|install\|info` | ✅ Production | `--json`, `--debug`, benchmark SLO |
| `tw create <n> --template=...` | ✅ Production | next-app, vite-react, vite-vue, vite-svelte, simple |
| `tw dashboard --port=...` | ✅ Production | Direct spawn server.mjs, tidak perlu build |
| `tw test [--watch]` | ✅ Production | Pass ke npm test |
| `tw storybook` | ✅ Production | Launch Storybook dev server; `--variants` untuk CI mode |
| `tw code --docs\|--install` | ✅ Production | VS Code extension docs + install |
| `tw preflight [--fix]` | ✅ Production | **Baru v4.4** — 8 environment checks |
