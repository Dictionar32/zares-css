{
  "name": "@tailwind-styled/shared",
  "version": "5.0.4",
  "description": "Shared utilities and types for tailwind-styled-v4",
  "license": "MIT",
  "type": "commonjs",
  "sideEffects": false,
  "engines": {
    "node": ">=20"
  },
  "main": "./dist/index.js",
  "module": "./dist/index.mjs",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.mjs",
      "require": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "dependencies": {
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/node": "^20",
    "tsup": "^8",
    "typescript": "^6"
  },
  "scripts": {
    "build": "tsup --config tsup.config.ts && node scripts/fix-require.js",
    "build:dev": "tsup --config tsup.config.ts --sourcemap --watch",
    "build:release": "tsup --config tsup.config.ts --no-sourcemap && node scripts/fix-require.js",
    "check": "tsc --noEmit -p tsconfig.json",
    "clean": "node -e \"require('node:fs').rmSync('dist',{recursive:true,force:true})\"",
    "test": "node --test tests/*.test.mjs",
    "test:watch": "node --test --watch tests/*.test.mjs",
    "pack:check": "node ../../scripts/check-pack-artifacts.mjs packages/domain/shared"
  }
}