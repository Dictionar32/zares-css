/**
 * @tailwind-styled/shared — Auto-Generated Schemas
 *
 * File ini akan berisi Zod schemas yang di-generate dari Rust structs
 * via `cargo run --bin export-schemas` + `npx tsx scripts/generate-json-schemas.ts`.
 *
 * Status: BELUM DIISI — menunggu implementasi `export-schemas` Rust binary.
 *
 * Setelah binary tersedia, file ini akan berisi:
 *   export { ScanResultSchema } from "./scanResult.schema"
 *   export { AnalyzerReportSchema } from "./analyzerReport.schema"
 *   export { NativeScanFileSchema } from "./nativeScanFile.schema"
 *   ... dst
 *
 * Lihat: plans/PLAN.md Wave 1 untuk roadmap lengkap.
 */

// Placeholder exports agar import tidak gagal sebelum schemas di-generate
export const SCHEMAS_GENERATED = false
export const SCHEMAS_VERSION = "0.0.0"
